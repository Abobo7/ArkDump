name: Patch and Test APK

on:
  workflow_dispatch:

jobs:
  patch-and-test:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: '3.x'

    - name: Set up Java
      uses: actions/setup-java@v2
      with:
        distribution: 'adopt'
        java-version: '11'

    - name: Install dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y wget unzip android-tools-adb android-tools-fastboot apktool aapt apksigner zipalign cpu-checker
        pip install objection frida-tools setuptools

    - name: Setup Docker
      run: |
        sudo apt-get remove -y docker docker-engine docker.io containerd runc || true
        sudo apt-get update
        sudo apt-get install -y ca-certificates curl gnupg
        sudo install -m 0755 -d /etc/apt/keyrings
        curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /etc/apt/keyrings/docker.gpg
        sudo chmod a+r /etc/apt/keyrings/docker.gpg
        echo \
          "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/ubuntu \
          $(. /etc/os-release && echo "$VERSION_CODENAME") stable" | \
          sudo tee /etc/apt/sources.list.d/docker.list > /dev/null
        sudo apt-get update
        sudo apt-get install -y docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin

    - name: Download APK
      run: |
        wget -O arknights.apk "https://ak.hycdn.cn/apk/202410301849-2401-te3svuc43h63yfe8a4pk/arknights-backup-328-2401.apk"

    - name: Patch APK
      run: |
        objection patchapk --source arknights.apk --architecture x86
        
    - name: Setup and run Android emulator
      run: |
        kvm-ok
        
        sudo docker pull budtmo/docker-android:emulator_12.0

        sudo docker run -d -p 6080:6080 -p 5554:5554 -p 5555:5555 -e EMULATOR_DEVICE="Samsung Galaxy S10" -e WEB_VNC=true --device /dev/kvm --name android-container budtmo/docker-android:emulator_12.0

        sleep 60

    - name: Install and test APK
      run: |
        adb devices
        adb install arknights.objection.apk
        
        cat > script.js << 'EOL'
        Java.perform(function() {
          console.log("Frida script loaded");
        });
        EOL
        
        frida -U -l script.js -f com.hypergryph.arknights

    - name: Cleanup
      if: always()
      run: |
        sudo docker stop android-container
        sudo docker rm android-container

    - name: Upload patched APK
      uses: actions/upload-artifact@v4
      with:
        name: patched-apk
        path: arknights.objection.apk
